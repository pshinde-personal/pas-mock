{
  "name": "mockoon",
  "version": "1.0.0",
  "description": "pas mock api center",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "mockoon-cli start --data db/dev.json db/prod.json --port 3003 3004",
    "stop": "mockoon-cli stop all"
  },
  "keywords": [
    "mock"
  ],
  "author": "Pratik Shinde",
  "license": "ISC",
  "dependencies": {
    "@mockoon/cli": "^2.0.0"
  }
}
